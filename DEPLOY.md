# QQ 机器人部署指南（超详细版）

> 本文档面向**完全没有编程基础**的 Windows 用户，从零开始，一步一步带你把 Platoboost QQ 机器人跑起来。
> 每一步都写清楚了「做什么、为什么、出错怎么办」。跟着做就行。
>
> 机器人功能：用户私聊或群里 @ 它发 Platoboost 链接，机器人自动求解并回复结果。

---

## 目录

1. [部署前你需要准备什么](#1-部署前你需要准备什么)
2. [一键部署脚本到底做了什么](#2-一键部署脚本到底做了什么)
3. [Windows 一键部署（推荐路线）](#3-windows-一键部署推荐路线)
4. [一键脚本 8 步环境检测详解](#4-一键脚本-8-步环境检测详解)
5. [在 QQ 开放平台创建机器人](#5-在-qq-开放平台创建机器人)
6. [手动安装（Windows / Linux / macOS）](#6-手动安装)
7. [填写配置文件](#7-填写配置文件)
8. [启动与验证](#8-启动与验证)
9. [使用方法](#9-使用方法)
10. [配置项详解](#10-配置项详解)
11. [故障排查大全](#11-故障排查大全)
12. [常见问题 FAQ](#12-常见问题-faq)
13. [架构说明](#13-架构说明)

---

## 1. 部署前你需要准备什么

| 项目 | 要求 | 说明 |
|---|---|---|
| 操作系统 | Windows 10 / 11 | 一键脚本只支持 Windows；Linux/macOS 走手动安装 |
| Python | **3.10 ~ 3.12** | 3.13 及以上暂不支持（numba 还没适配），3.9 及以下太旧 |
| 磁盘空间 | 至少 2 GB | 依赖库（numpy/scipy/numba）加起来约 1 GB |
| 网络 | 能访问 GitHub 和 `api.sgroup.qq.com` | 前者下载源码，后者是机器人 API |
| QQ 账号 | 已实名认证 | 用于注册 [QQ 开放平台](https://q.qq.com) 并创建机器人 |
| 时间 | 约 15~30 分钟 | 大部分时间在等依赖下载 |

**怎么确认 Python 装没装、版本对不对：**

1. 按 `Win + R`，输入 `cmd`，回车，打开黑色命令行窗口。
2. 输入 `python --version` 回车。
3. 显示 `Python 3.12.x` → 合格，直接跳到第 3 节。
4. 显示 `'python' 不是内部或外部命令` → 没装或没加 PATH，见下方「安装 Python」。
5. 显示 `Python 3.13.x` 或更高 → 版本过新，需要装 3.12。
6. 显示 `Python 2.x` 或 `Python 3.8.x` → 太旧，需要升级。

**安装 Python（没装或版本不对时）：**

1. 打开 https://www.python.org/downloads/
2. 找 **Python 3.12.x** 的 `Windows installer (64-bit)` 下载。
3. 双击运行安装程序。**第一屏最下方有个复选框 "Add python.exe to PATH"，务必勾上！** 这是 90% 新手翻车的地方。
4. 点 `Install Now`，等它装完。
5. **关闭所有已打开的命令行窗口**（PATH 要重开窗口才生效），重新按 `Win + R` → `cmd` → `python --version` 确认。

> ⚠️ 如果系统里已有多个 Python，建议先卸载旧的再装 3.12，避免脚本检测到错误版本。

---

## 2. 一键部署脚本到底做了什么

`install_windows.bat` 是一个 Windows 批处理脚本，双击运行后会自动完成以下 8 步（详见第 4 节）：

1. ✅ **检测目录写权限** —— 没权限会告诉你三种解决办法
2. ✅ **检测 Python** —— 没装/版本不对/装了跑不起来，都会给出对应处理指引
3. ✅ **检测网络** —— 分别测试 GitHub（下源码）和 QQ API（机器人通信）连通性
4. ✅ **获取源代码** —— 有 git 用 git，没有就自动下 zip 解压；已有源码则跳过
5. ✅ **创建虚拟环境** —— 独立的 `venv\` 目录，不污染系统 Python
6. ✅ **安装依赖** —— 阿里云镜像加速，失败自动切官方源重试
7. ✅ **生成并检查配置** —— 自动生成 `qq_config.json`，没填密钥会弹记事本让你填
8. ✅ **询问是否启动** —— 填完配置直接 `y` 回车就能跑

**脚本设计原则：任何一步失败都不会继续往下走，而是停下来告诉你：**
- 出了什么错（中文说明）
- 为什么会出这个错
- 怎么修（给出具体操作步骤）

所以遇到红色 `[错误]` 不要慌，照着提示做，修完**重新双击脚本**即可——已完成的步骤会自动跳过，不会重复下载。

---

## 3. Windows 一键部署（推荐路线）

### 3.1 下载脚本

**方式 A（推荐）：下载整个仓库**

1. 打开仓库页面：https://github.com/2233qazwsx0/platoboost-qq-bot
2. 点绿色的 **`<> Code`** 按钮 → **`Download ZIP`**。
3. 把 ZIP 解压到一个**你能写入的目录**，例如 `D:\qq-bot\`（解压后里面应有 `install_windows.bat`）。

**方式 B：只下载脚本文件**

1. 打开仓库里的 `install_windows.bat` 文件页面。
2. 点右上角下载图标（或 Raw 另存为），保存到一个空文件夹。

> ⚠️ 之前有用户把文件放到 `D:\qq bot\` 这种受保护目录导致「拒绝访问」，建议放在 `D:\` 根目录下自建的文件夹，或你的用户目录里。

### 3.2 运行脚本

**双击 `install_windows.bat`**。

- 如果双击后窗口一闪而过：说明脚本在第一步就报错了，右键脚本 → 「以管理员身份运行」再看输出。
- 正常情况会看到标题为「Platoboost QQ 机器人 - 一键部署（环境检测版）」的窗口，逐步显示 `[1/8]` 到 `[8/8]`。

### 3.3 中途会发生什么

- **步骤 6 安装依赖最慢**（2~5 分钟），会滚动大量英文下载进度，**属正常现象，别关窗口**。
- **步骤 7 可能弹出记事本**：这是让你填机器人的 AppID/AppSecret（如果还没填）。填写方法见第 5、7 节。填完 `Ctrl+S` 保存，关闭记事本，回到黑窗口继续。

### 3.4 启动

最后一步问 `现在启动机器人吗？(y/n)`：

- 输入 `y` 回车 → 直接启动，看到 `[INFO] 已连接网关, 机器人已就绪` 就成功了。
- 输入 `n` 回车 → 以后想启动，双击 `start_windows.bat` 即可。

---

## 4. 一键脚本 8 步环境检测详解

这一节解释每一步检测什么、失败输出什么、怎么处理。**建议遇到报错时对照本节排查。**

### 步骤 1/8：检测目录写权限

**检测内容**：在脚本所在目录尝试创建一个临时文件。

**失败原因**：当前 Windows 用户对这个文件夹没有写入权限。常见于把文件放在受保护的系统目录、或某些 D 盘特殊分区。

**解决办法（三选一）**：
- A. 右键脚本 → 以管理员身份运行
- B. 右键文件夹 → 属性 → 安全 → 编辑 → 选中你的用户名 → 勾选「修改」和「写入」→ 确定
- C. 把整个文件夹移到别的位置（如 `D:\` 根目录下新建文件夹）再运行

### 步骤 2/8：检测 Python

**检测内容**：依次找 `py` 启动器和 `python` 命令；找到后获取版本号并检查是否在 3.10~3.12 区间。

**各种失败情形**：

| 输出 | 原因 | 处理 |
|---|---|---|
| `未检测到 Python` | 完全没装，或装了没勾 "Add to PATH" | 去 python.org 装 3.12，**勾选 Add python.exe to PATH**，装完重开窗口重跑脚本 |
| `找到了 py 但无法获取版本号` | Python 安装损坏 | 控制面板卸载后重装 3.12 |
| `需要 Python 3.x，当前是 2.x` | 系统里是老 Python 2 | 卸载旧的，装 3.12 |
| `版本过新，numba/scipy 装不上` | 用了 3.13+ | 装 3.12 并替换 |
| `偏旧（建议 3.10~3.12）` | 3.9 及以下 | 只是警告会继续跑；如果后面依赖装失败，回来换 3.12 |

> 💡 `py` 是 Windows 官方的 Python 启动器，装 Python 时自动带上。脚本优先用它，因为它能正确处理多版本共存。

### 步骤 3/8：检测网络

**检测内容**：用 PowerShell 的 `Test-NetConnection` 分别测试 `github.com:443` 和 `api.sgroup.qq.com:443`。

**输出解读**：

- GitHub 不通 → 只是**警告**，脚本继续。如果步骤 4 下载失败，手动到仓库页面点 Code → Download ZIP，解压到脚本目录后重跑（脚本检测到 `qq_bot.py` 存在会跳过下载）。
- QQ API 不通 → 只是**警告**，脚本继续。但机器人启动后可能收发不了消息，需要检查防火墙/代理/公司网络。

> 💡 公司内网或开了代理软件时，GitHub 检测可能误报。以步骤 4 实际下载结果为准。

### 步骤 4/8：获取源代码

**检测内容**：目录里有没有 `qq_bot.py`（有就跳过下载）；没有则优先用 git 克隆，没装 git 就用 PowerShell 下载 zip 解压。

**失败处理**：

- git 克隆失败 → 脚本自动降级为 zip 方式，不用管。
- zip 下载失败 → 检查网络；或手动下载 ZIP 解压到当前目录后重跑脚本。

### 步骤 5/8：创建虚拟环境

**检测内容**：`venv\Scripts\python.exe` 是否存在；不存在则用步骤 2 找到的 Python 创建。

**为什么要虚拟环境**：机器人依赖的库和系统里其他 Python 程序可能冲突，venv 把所有东西关在项目文件夹里，删文件夹 = 完全卸载，干净。

**失败处理**：基本只有权限问题（回到步骤 1 的三种办法）或 Python 损坏（重装）。

### 步骤 6/8：安装依赖

**检测内容**：用 venv 里的 pip 从阿里云镜像装 `requirements.txt` 里的所有库；失败自动换官方源重试。

**这一步最慢也最容易出问题**：

| 现象 | 处理 |
|---|---|
| 下载进度条走走停停 | 正常，镜像同步延迟，耐心等 |
| `numba` 装失败 | Python 版本问题，回步骤 2 换 3.10~3.12 |
| 阿里云和官方源都失败 | 检查网络；公司网可能封了 pypi，换手机热点试 |
| 磁盘满 | 清理出至少 2 GB 空间 |

### 步骤 7/8：生成并检查配置

**检测内容**：`qq_config.json` 不存在则生成模板；存在则检查 `app_id`/`app_secret` 是否已填，没填会弹记事本。

**注意**：如果你之前填过配置，脚本**不会覆盖**，放心重跑。

### 步骤 8/8：启动

问你要不要立即启动。选 `y` 直接跑；选 `n` 以后用 `start_windows.bat`。

---

## 5. 在 QQ 开放平台创建机器人

这一步拿到机器人的「身份证」，一键脚本第 7 步弹记事本时需要填。

1. 打开 https://q.qq.com ，用 QQ 扫码登录，完成开发者注册（**个人主体**即可，不用企业资质）。
2. 进入控制台，点 **创建机器人**：
   - 名称、头像、简介随意填（这是用户在 QQ 里看到的机器人形象）。
3. 进入机器人详情页，记下两个值：
   - **AppID**：机器人详情页顶部的一串数字。
   - **AppSecret**：开发设置 → 密钥 → 点「获取」后显示（只显示一次，及时复制）。
4. 开发设置 → **沙箱配置**：把**你自己的 QQ 号**添加为沙箱成员。
   > 机器人没上线前，**只有沙箱成员能和它对话**。不加这个，你发消息机器人不会理你。
5. 机器人能力：开启 **C2C（单聊）** 和 **群聊** 能力。如果平台要求提交申请，按提示提交（个人开发者的工具类机器人一般能过）。

> 📌 正式上线需要审核。上线前用沙箱模式调试完全够用；上线后把配置里 `sandbox` 改为 `false`。

---

## 6. 手动安装

适合 Linux/macOS 用户，或 Windows 用户想手动控制每一步的情况。

```bash
# 1. 获取源码
git clone https://github.com/2233qazwsx0/platoboost-qq-bot.git
cd platoboost-qq-bot
# 没有 git：到仓库页面 Code → Download ZIP，解压后进入目录

# 2. 创建虚拟环境
python -m venv venv
# Windows 激活：
venv\Scripts\activate
# Linux / macOS 激活：
source venv/bin/activate

# 3. 安装依赖（国内用阿里云镜像更快）
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 4. 生成配置（首次运行会自动生成模板）
python qq_bot.py
# 提示"请填写 app_id/app_secret"即正常，编辑 qq_config.json 后再运行
```

---

## 7. 填写配置文件

用任意编辑器（记事本即可）打开项目目录下的 `qq_config.json`：

```json
{
  "app_id": "123456789",
  "app_secret": "abcdef1234567890abcdef",
  "sandbox": true,
  "solve_workers": 4,
  "user_cooldown": 10
}
```

| 字段 | 填什么 |
|---|---|
| `app_id` | 第 5 节拿到的 AppID（纯数字，引号里） |
| `app_secret` | 第 5 节拿到的密钥（点「获取」后复制的那串） |
| `sandbox` | **未上线机器人必须 `true`**；正式上线后改 `false` |
| `solve_workers` | 并发求解线程数，默认 4，一般不用改 |
| `user_cooldown` | 同一用户两条消息最小间隔秒数，防刷屏 |
| `admins` | 管理员 openid 数组；首个管理员用 `/whoami` 查到后手工填入 |

**填写注意事项：**

- 值必须包在**英文双引号**里（`"123456789"` ✅，`123456789` ❌）
- 引号、冒号、逗号都要是**英文半角**符号，不能用中文标点
- 填完保存时确认编码为 UTF-8（记事本默认就是）

⚠️ `app_secret` 是机器人的密码，**不要发给别人、不要提交到 git**（`.gitignore` 已排除）。

---

## 8. 启动与验证

```bash
# Windows（双击 start_windows.bat 效果相同）
venv\Scripts\python qq_bot.py

# Linux / macOS（venv 激活状态下）
python qq_bot.py
```
（Windows 直接双击 `start_windows.bat`，机器人崩溃/更新后会自动重启）

**正常启动输出：**

```
[INFO] 已连接网关, 机器人已就绪
```

**验证步骤：**

1. 用**沙箱成员**的 QQ 号（第 5 节第 4 步加的那个）在 QQ 里搜索机器人名称。
2. 私聊发一条 `你好`，机器人应回复使用说明。
3. 发一条 Platoboost 链接（`/key https://auth.platorelay.com/a?d=xxxx`），约 7~10 秒后回复求解结果。

> ⚠️ 注意用 `venv\Scripts\python` 而不是系统的 `python`——依赖装在 venv 里，系统 Python 找不到会报 `ModuleNotFoundError`。

**认领管理员**：给机器人发 `/whoami`，把回复里的 openid 填进 `qq_config.json` 的 `"admins": ["你的openid"]`，重启后即可使用管理命令。

---

## 9. 使用方法

| 场景 | 操作 | 机器人响应 |
|---|---|---|
| 私聊 | 发 `/key <链接>` | "正在解卡😘" → "解卡成功 + 卡密 + by CUA" |
| 群聊 | @机器人 + `/key <链接>` | 同上 |
| 任何人 | `/whoami` | 回复自己的 openid（用于认领管理员） |
| 管理员 | `/status` | 运行时长/WS 状态/缓存数/求解中/管理员数 |
| 管理员 | `/update` | 从 GitHub 拉最新代码，有变更则自动重启生效 |
| 管理员 | `/restart` | 手动重启 |
| 管理员 | `/admin add <openid>` / `/admin rm <openid>` / `/admin ls` | 管理员增删查 |
| 私聊/群 | 发其他 `/xxx` 命令 | 使用说明 |
| 私聊/群 | 发不以 / 开头的普通文字 | 不回复 |

命令格式：`/key https://auth.platorelay.com/a?d=xxxx`（`/key` 和链接之间有空格）。

回复示例：

```
正在解卡😘

解卡成功
FREE_82e8004b6bd3a9ed5da5d2c60adc5d4f

by CUA
```

失败则回复 `解卡失败: <原因>`。

---

## 10. 配置项详解

- **`sandbox`**：沙箱模式走 `sandbox.api.sgroup.qq.com`，只有沙箱成员可见可对话。机器人通过审核正式上线后改为 `false`，走正式 API。
- **`solve_workers`**：同时求解的线程上限。每个求解约占 1 核几秒钟，CPU 差就调小到 2。
- **`user_cooldown`**：同一用户发第二条消息的间隔限制，期间的消息直接忽略（不回复）。
- 频控说明：QQ 官方限制被动回复群消息 5 分钟 5 条、私聊 60 分钟 4 条。本机器人每次求解只回复 2 条（受理 + 结果），正常使用不会触发。

---

## 11. 故障排查大全

### 安装阶段

| 现象 | 原因与解决 |
|---|---|
| `[WinError 5] 拒绝访问` | 目录没写权限。管理员运行脚本，或给目录加权限（见第 4 节步骤 1） |
| `两条命令被粘到一行` 报错 | 复制命令时把换行弄丢了。**一行一条**分开执行，或直接用一键脚本 |
| `ModuleNotFoundError: No module named 'websocket'` | 用了系统 Python 而不是 venv。改用 `venv\Scripts\python qq_bot.py`；若 venv 里也没有，`venv\Scripts\pip install websocket-client` |
| pip 装 numba 失败 | Python 版本 > 3.12，换 3.10~3.12 |
| 源码下载失败 | 网络不通 GitHub。手动 Download ZIP 解压到目录后重跑脚本 |

### 运行阶段

| 现象 | 原因与解决 |
|---|---|
| `请先填写 app_id / app_secret` | 配置没填或填错，回到第 7 节 |
| 连接网关后立刻断开 | AppSecret 错误，重新复制；或 `sandbox` 设了 `true` 但机器人已上线（改 `false`） |
| `获取 access_token 失败` | app_id 或 app_secret 填错，去管理后台重新复制 |
| 机器人不回消息 | ① 你的 QQ 号不在沙箱名单 ② 群里没 @ 机器人 ③ 机器人未开启 C2C/群聊能力 ④ `sandbox` 与机器人上线状态不匹配 |
| `求解失败: 网络错误` | 服务器访问不了 `auth.platorelay.com`，检查出网 |
| 长时间无输出 | 看终端日志，`[WARN]` 是可恢复重连，`[ERROR]` 需按提示处理 |

---

## 12. 常见问题 FAQ

**Q：双击 bat 后窗口一闪而过？**
A：脚本第一步就出错退出了。右键 → 以管理员身份运行，窗口会停住显示错误信息。

**Q：重跑一键脚本会把我的配置覆盖吗？**
A：不会。脚本检测到 `qq_config.json` 已存在会保留原文件，只检查密钥填没填。

**Q：重跑会重复下载依赖吗？**
A：pip 会跳过已装好的包，只补缺失的，很快。

**Q：能把文件夹挪到别的盘吗？**
A：能，但挪完后 venv 里的路径会失效，需要删掉 `venv` 文件夹后重跑一键脚本重建（配置会保留）。

**Q：想完全卸载？**
A：直接删掉整个项目文件夹即可（虚拟环境都在里面，不留垃圾）。

**Q：电脑关机后机器人还在吗？**
A：不在。机器人跑在你电脑上，关机/关窗口就下线。想 24 小时在线需要部署到云服务器（Linux 手动安装流程同样适用）。

---

## 13. 架构说明

```
QQ 用户 ──(WS 网关)──> qq_bot.py ──> server.run_solves()
                                    ├── auth_client.py   拉取页面/提取票据
                                    ├── captcha_solver.py 验证码识别(numba 加速)
                                    └── 结果缓存(同链接 30 分钟内直接返回)
```

- `qq_bot.py`：QQ 官方机器人客户端（WebSocket 长连接、鉴权、心跳、消息分发）
- `server.py`：求解调度 + 缓存（HTTP API 模式也在这里，`python server.py` 可单独跑 API 服务）
- `auth_client.py`：Platoboost 页面拉取、AES 解密、票据提取
- `captcha_solver.py`：滑块验证码图像识别

本项目基于 [AbabaHnb/Delta-bypass](https://github.com/AbabaHnb/Delta-bypass) 二次开发，新增 QQ 机器人接入。